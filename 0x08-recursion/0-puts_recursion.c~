#include "holberton.h"

/**
 * _puts_recursion - print string with recursive function
 * @s: string to print
 * Return: do not return
 */
void _puts_recursion(char *s);
{
	if (*s == '\0')
	{
		_putchar('\n');
	}
	else
	{
		_putchar(*s);
		_puts_recursion(s + 1);
	}
}
