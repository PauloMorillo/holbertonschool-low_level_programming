#include "holberton.h"

/**
 * _isupper - function to know if a character is upper
 * @c: - input int parameter
 * Return: - return 1 if the input value is an uppercase otherwise returns 0
 */

int _isupper(int c)
{
	if (c >= 64 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
