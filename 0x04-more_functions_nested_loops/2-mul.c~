#include "holberton.h"

/**
 * mul - function to know if a character is a digit
 * @a: - input int parameter first number
 * @b: - input int parameter second number
 * Return: - return the answer to multiply a with b
 */

int mul(int a, int b)
{
	int ans = a * b;
	return (ans);
}
