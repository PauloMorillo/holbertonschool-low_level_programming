#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n = 'a';
	char n2 = 'A';

	while (n <= 'z')
	{
		putchar(n);
		n = n + 1;
	}
	while (n2 <= 'Z')
	{
		putchar(n2);
                n2 = n2 + 1;
	}
	putchar('\n');
	return (0);
}
