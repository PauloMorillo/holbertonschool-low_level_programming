#include "holberton.h"
/**
 * add - returns add of two integers.
 *@a: first input number
 *@b: second input number
 *Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int a, int b)
{
	return (a+b);
}
